spring:
  application:
    name: USER-SERVICE


server:
  port: 8081
  ssl:
    enabled: false


# Eureka (Discovery)
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
