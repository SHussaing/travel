server:
  port: 8084

spring:
  application:
    name: AUTH-SERVICE
  data:
    mongodb:
      uri: ${SPRING_MONGODB_URI:${MONGODB_URI:mongodb://localhost:27017/traveldb}}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka/}
